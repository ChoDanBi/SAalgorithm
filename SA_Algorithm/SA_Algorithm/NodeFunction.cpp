#include <cmath>
#include "NodeFunction.h"
#include "NodeManager.h"

void NodeFunction::Init(vector<Vec2*>* Relay, vector<int, int>* Matching)
{
	vector<int> chk(NM->GetSensor()->size(), false);
	while(true) {




	}
}

bool NodeFunction::Linked(Vector2 _vec, int _num)
{
	


}

